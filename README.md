# Knowledge Team Premium Dashboard

Open `index.html` in VS Code with Live Server, or double-click it.

The sign-up profile is saved with localStorage and sessionStorage fallback, so the entered details do not disappear when the page is refreshed.

Mentoring includes all requested teams, generated staff-control commands, roles, name changes, copy buttons, and a completion checklist.
